apply plugin: "war"

bootWar {
    mainClass = "com.alphadevs.wikunum.services.PaymentManagementApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
